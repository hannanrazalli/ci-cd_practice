version: 2

sources:
  - name: transactions_source
    database: transactions-practice
    schema: bronze  # Sesuai dengan destination_table='bronze.raw_transactions' dalam Python
    tables:
      - name: raw_transactions
        columns:
          - name: txn_id
            tests:
              - not_null
              # - unique